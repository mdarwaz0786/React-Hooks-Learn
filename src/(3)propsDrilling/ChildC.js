import React from 'react';

const ChildC = ({ name, age }) => {
    return (
        <div>
            <p>ChildC return my name is {name} and my Age is {age}.</p>
        </div>
    );
};

export default ChildC;